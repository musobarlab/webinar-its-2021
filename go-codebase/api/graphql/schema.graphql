schema {
    query: Query
    mutation: Mutation
}

type Query {
    auth: Auth
}

type Mutation {
    auth: Auth
}

scalar Time

type Meta {
    page: Int!
    limit: Int!
    totalRecords: Int!
    totalPages: Int!
}

input Filter {
    page: Int!
    limit: Int!
    sort: String!
    sortBy: String!
}
